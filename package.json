{
  "name": "firstroll-web",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "sh tools/build_web.sh",
    "build:auth": "esbuild app/web/auth.js --bundle --minify --format=iife --outfile=dist/assets/auth.js"
  },
  "dependencies": {
    "@azure/msal-browser": "5.19.0",
    "@supabase/supabase-js": "2.112.3"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
