# tools/release — FirstRoll backend release approval system.
#
# This package provides:
#   manifest       — structured release manifest data model
#   risk           — deterministic risk classification
#   authorization  — single-use HMAC-signed approval capabilities
#   summary        — human-readable approval summary generation
#   audit          — durable audit event recording
#   broker         — trusted approval broker (standalone FastAPI service)
